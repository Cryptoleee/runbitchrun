{
  "name": "runbitchrun",
  "version": "1.0.0",
  "description": "",
  "main": "sw.js",
  "scripts": {
    "build": "rm -rf www && mkdir -p www && rsync -a --exclude=www --exclude=node_modules --exclude=ios --exclude=.git --exclude=.DS_Store . www/",
    "cap:sync": "npm run build && npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Cryptoleee/runbitchrun.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Cryptoleee/runbitchrun/issues"
  },
  "homepage": "https://github.com/Cryptoleee/runbitchrun#readme",
  "dependencies": {
    "@capacitor-firebase/authentication": "^8.1.0",
    "@capacitor/cli": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/ios": "^8.2.0",
    "@capacitor/status-bar": "^8.0.1"
  }
}
